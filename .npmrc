# Workspace packages are consumed from source through their `exports`; hoisting
# keeps a single copy of react, next and drizzle for every consumer.
shamefully-hoist=false
confirmModulesPurge=false
verify-deps-before-run=false

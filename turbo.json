{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "typecheck": {},
    "test": {
      "cache": false,
      "env": ["DATABASE_URL", "DATABASE_ADMIN_URL", "CLICKHOUSE_*"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
